# Molecule CI dependencies for the shared dryvist/.github _ansible-ci.yml gate.
#
# The shared molecule job installs these via `uv pip install --system -r`. This
# file lives in the repo (not the shared workflow) so this repo owns its driver
# and version pins. Mirrors the deps the retired local _molecule.yml installed.
docker>=7.0.0
urllib3<2.0
requests<2.32
molecule
molecule-plugins[docker]>=23.0.0
ansible-core>=2.18
ansible-lint
yamllint
