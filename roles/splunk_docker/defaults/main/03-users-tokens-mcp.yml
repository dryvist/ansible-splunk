---
# --- Managed service users + auth tokens -----------------------------------
# Create dedicated Splunk users (beyond the built-in admin) and mint each an
# authorization token, so AI agents / MCP clients get their own scoped REST +
# search identity instead of sharing the admin password. Off by default; the
# splunk inventory group enables it and supplies the user list.
splunk_docker_manage_users: false

# List of managed users. Each: { name, roles: [..] }.
#   name  - Splunk username (also the token owner / audience owner).
#   roles - Splunk roles granted (e.g. [admin], [user], [power]).
# The user's password is randomly generated once at creation and never rotated
# here — the minted token is the credential that clients actually use.
splunk_docker_users: []

# Custom roles, reconciled before the users that reference them.
#
# A minted token inherits its user's roles, so the role IS the grant. Declaring
# one is how a service identity gets exactly what its work needs instead of
# whichever built-in is closest — and neither obvious built-in fits an
# observing identity: `admin` can administer the deployment it observes, and
# `user` reaches no internal index (Splunk's "*" matches all non-internal OR
# all internal, never both) and lacks the capability the MCP app requires to
# execute tools at all.
#
# Per entry:
#   name                 - role name
#   imported_roles       - roles whose grants are inherited (e.g. [user])
#   srch_indexes_allowed - searchable indexes; "_*" is required for internal
#   srch_indexes_default - searched when a query names no index
#   capabilities         - capabilities granted on top of the imported roles
splunk_docker_manage_roles: true
splunk_docker_roles: []

# Token audience tag used to inventory enabled tokens whose not-before and
# expiry windows include the current time. Audience is DICTATED by the MCP app
# (its auth only accepts mcp), not a free-form label.
splunk_docker_token_audience: "mcp"

# Token owners that are deliberately NOT managed users. Anything listed here is
# exempt from the undeclared-token gate, so each entry needs a reason: the gate
# exists because reconciliation only visits declared users, and an exemption
# puts an owner permanently outside every rotation and cleanup path.
splunk_docker_token_owner_exceptions: []

# Relative expiry for minted tokens (Splunk relative-time syntax, e.g. +90d).
# Empty string omits expires_on so Splunk applies its default token lifetime.
# 47d matches this estate's rotation hard ceiling (openbao_rotation_max_days
# in ansible-proxmox-apps) rather than Splunk's own 90d default.
splunk_docker_token_expires_on: "+47d"

# When true, publish the shared MCP connection to OpenBao. Requires a
# write-capable OpenBao AppRole in the converge environment (see the
# *_openbao_* vars below), including KV-v2 data read/write, metadata read, and
# undelete access for the exact secret path. Inert by default: the user is
# created, but a token is minted only when it can be written to the canonical
# shared secret.
splunk_docker_token_publish_openbao: false
# Explicit one-run rotation control. When true, mint and CAS-publish exactly one
# replacement, then re-enumerate and revoke every other eligible user/audience
# token. Keep false for normal converges, which still repair a missing or
# invalid published token.
splunk_docker_token_force_rotate: false
splunk_docker_token_openbao_addr: "{{ lookup('env', 'BAO_ADDR') | default('', true) }}"
splunk_docker_token_openbao_token: "{{ lookup('env', 'BAO_TOKEN') | default('', true) }}"
splunk_docker_token_openbao_mount: "secret"
splunk_docker_token_openbao_path: "ai/mcp/splunk"
splunk_docker_url_openbao_key: "SPLUNK_MCP_URL"
splunk_docker_token_openbao_key: "SPLUNK_MCP_TOKEN"
splunk_docker_mcp_host: >-
  {{ (hostname ~ '.' ~ tofu_data.domain)
     if (hostname is defined and tofu_data is defined and tofu_data.domain is defined)
     else ansible_host }}
# Published URL = the Traefik-fronted `splunk-mgmt` ingress (a first-class
# tofu ingress entry), NOT the raw https://<host>:<mgmt_port> endpoint. The
# raw mgmt port serves Splunk's self-signed cert, which MCP clients rightly
# refuse — the Hermes gateway parked its splunk MCP server on exactly this
# ("failed initial connection ... parking") while the token itself was valid,
# leaving the SIEM blind for cron monitoring (2026-07-18, Zammad incident).
# Traefik terminates real TLS on the ingress name; publishing that URL makes
# every consumer verify-clean by default.
#
# The ingress base is PROXMOX_SUBDOMAIN — the same value the traefik role
# serves its routers under — NOT tofu_data.domain. tofu_data.domain is the
# estate apex, where only the Proxmox nodes and their load balancer live;
# `splunk-mgmt.<apex>` resolves to nothing that answers, and publishing it
# left every MCP consumer pointed at a dead name (2026-09-11). Fall back to
# the raw mgmt URL only when the converge env carries no ingress base.
splunk_docker_mgmt_base_url: >-
  {{ ('https://splunk-mgmt.' ~ lookup('env', 'PROXMOX_SUBDOMAIN'))
     if lookup('env', 'PROXMOX_SUBDOMAIN')
     else 'https://' ~ splunk_docker_mcp_host ~ ':' ~ splunk_docker_mgmt_port }}
splunk_docker_mcp_url: "{{ splunk_docker_mgmt_base_url }}/services/mcp"

# --- MCP-token rotator identity ---------------------------------------------
# A dedicated Splunk user that ONLY the OpenBao-side scheduled rotator
# (ansible-proxmox-apps roles/openbao/templates/rotators/splunk-mcp.sh.j2)
# authenticates as, via HTTP basic auth, to mint/revoke MCP tokens for OTHER
# users (currently `hermes`) on a schedule — separate from the per-converge
# minting this role already does in manage_one_user.yml. Off by default; the
# splunk inventory group turns it on.
#
# Deliberately NOT a member of splunk_docker_users: that list's reconciliation
# mints an MCP token for every member and CAS-publishes it to the ONE shared
# secret at splunk_docker_token_openbao_path — a second member would clobber
# the first's published token. This identity never mints a token for itself
# (edit_tokens_all lets it mint for others without needing tokens of its own),
# so it is also correctly absent from audit_undeclared_tokens.yml's declared
# set: it is expected to hold zero live tokens.
splunk_docker_manage_mcp_rotator: false
splunk_docker_mcp_rotator_user: "svc-mcp-rotator"
# Grants ONLY edit_tokens_all (create/view/manage tokens for any user) —
# nothing broader. No imported_roles: this identity never searches, so it
# gets no index access at all, unlike hermes_search.
splunk_docker_mcp_rotator_role: "mcp_token_minter"
# secret/apps/splunk-rotator — the exact path + field names
# (mgmt_url/username/password) the splunk-mcp-rotate AppRole's minter_secret
# already expects (ansible-proxmox-apps defaults/main/09-snapshots-and-rotation.yml).
# Mount reuses splunk_docker_token_openbao_mount; the rotator script forms its
# own KV data path as "<mount>/data/apps/splunk-rotator".
splunk_docker_mcp_rotator_openbao_path: "apps/splunk-rotator"
