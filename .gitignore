*.retry
*.pyc
__pycache__/
.pytest_cache/
.venv/
venv/
*.egg-info/
dist/
build/
.tmp/
.idea/
*.swp
*.swo
*~
.DS_Store
.fact_cache/
/tmp/ansible_facts/

# Large Splunk add-on files (must be placed manually)
# See roles/splunk_docker/files/README.md for instructions
roles/splunk_docker/files/*.tar
roles/splunk_docker/files/*.tgz
roles/splunk_docker/files/*.spl

# OpenTofu inventory (generated at runtime)
inventory/tofu_inventory.json

# Nix direnv cache
.direnv/

# Nix build output symlinks — `nix build` and some dev-shell refreshes
# leave `result` (and multi-output variants like `result-1`, `result-bin`,
# `result-dev`) at the repo root pointing into /nix/store. Never commit
# them; re-created on every build.
/result*

# Private AI assistant instructions (not committed to git)
.CLAUDE.local.md
CLAUDE.local.md
.ansible/
.direnv/
